// RUN: %clang_cc1 -verify %s

union SimpleUnion {
  float uf;
  int ui;
  char uc;
};

double union SimpleUnion::getA(union SimpleUnion * this); // expected-note {{previous declaration is here}}
int union SimpleUnion::getA(union SimpleUnion * this) { // expected-error {{conflicting types for 'getA'}}
  return this->ui;
}