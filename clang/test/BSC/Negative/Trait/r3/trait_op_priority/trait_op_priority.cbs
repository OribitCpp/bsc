// RUN: %clang_cc1 -ast-dump -verify %s
// expected-no-diagnostics

struct B {
    int a;
    int b;
};

struct A {
    struct B b;
    struct B * c;
    struct B d;
    struct A * e;
    struct A * f;
};

void func1(struct A a) {
    int res = (int)(*(a.c)).b;
    return;
}