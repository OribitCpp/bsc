// RUN: %clang_cc1 -fsyntax-only -verify %s

trait F<T> {
  T foo(This* this);
};

int int::foo(int* this) {
  return 10;
}

impl trait F<S> for int; // expected-error {{use of undeclared identifier 'S'}}