// RUN: %clang_cc1 -internal-isystem %S/../../../../../../lib/Headers/bsc_include -verify %s 

#include "../../async-func-read-opposite.hbs"

int p(int i) {
  return i;
}

async int f() {     
    int a = await p(1); // expected-error {{the await expression's external function declaration must be modified by async or return 'Future' type}}
    int b = await read(1);
    return b;
}

int main() {
  return 0;
}