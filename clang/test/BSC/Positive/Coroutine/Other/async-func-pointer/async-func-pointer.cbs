// RUN: %clang %s -o %test.output
// RUN: %test.output
// RUN: %clang -rewrite-bsc %S/../../../../../../lib/Headers/bsc_include/future.hbs -o %T/future.h
// RUN: %clang -rewrite-bsc %s -o %t-rw.c
// RUN: %clang %t-rw.c -o %t-rw.output
// RUN: %t-rw.output
// expected-no-diagnostics

#include "future.hbs"
#include <stdlib.h>

typedef trait Future<int>* (*pollFunc) (); // triger point 1, do not change

typedef struct EQueueFunc {
    pollFunc func;
} EQueueFunc;


async void f() { // triger point 2, do not change
    EQueueFunc f;
    await f.func();
}

int main() {
  return 0;
}