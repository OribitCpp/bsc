// RUN: %clang %s -o %test.output
// RUN: %test.output
// RUN: %clang -rewrite-bsc %S/../../../../../../../lib/Headers/bsc_include/future.hbs -o %T/future.h
// RUN: %clang -rewrite-bsc %s -o %t-rw.c
// RUN: %clang %t-rw.c -o %t-rw.output
// RUN: %t-rw.output
// expected-no-diagnostics

#include "future.hbs"
#include <stdlib.h>

async int int::g(int start) {
    return 0;
}

async int int::q(int* this) {
    return 0;
}

trait Future<int>* (*CallBack)(int) = int::g;
trait Future<int>* (*CallBack2)(int*) = int::q;

async int f(int start) {
  int result = await CallBack(1);
  result = await CallBack2(&result);
  return result;
}

int main() {
  return 0;
}