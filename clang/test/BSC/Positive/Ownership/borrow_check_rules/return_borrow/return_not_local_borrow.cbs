// RUN: %clang %s -o %test.output
// RUN: %test.output
// RUN: %clang -rewrite-bsc %s -o %t-rw.c
// RUN: %clang %t-rw.c -o %t-rw.output
// RUN: %t-rw.output
// expected-no-diagnostics


struct S {
  int *borrow a;
};

struct S f(struct S ss) {
  struct S *ptr = &ss;
  struct S t = *ptr;
  return t;
}

int main() {
  int b = 20;
  int *borrow p = &mut b;
  struct S s = {p};
  struct S s_r =  f(s);
  struct S *m = &s;
  struct S **n = &m;
  struct S k = *m;
  *k.a = 10;
  struct S j = **n;
}