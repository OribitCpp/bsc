// RUN: %clang %s -o %t.output
// RUN: %t.output
// RUN: %clang -rewrite-bsc %s -o %t-rw.c
// RUN: %clang %t-rw.c -o %t-rw.output
// RUN: %t-rw.output
// expected-no-diagnostics

enum sock_handle_type {
    SOCK_HANDLE_INIT = 0,
    SOCK_HANLE_LISTENER = 1
};

int s(enum sock_handle_type except) {
    if (except != SOCK_HANDLE_INIT) {

    }
    return 0;
}

int main() {
    return 0;
}
