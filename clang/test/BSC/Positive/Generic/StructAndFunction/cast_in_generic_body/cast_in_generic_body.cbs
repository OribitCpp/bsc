// RUN: %clang %s -o %t.output
// RUN: %t.output
// expected-no-diagnostics

int foo<T> (T a, T b)
{
  int x = int(a) + int(b); // this should report error
  return x;
}

int main ()
{
  float a = 2.3;
  float b = 1.1;
  int res = foo<float>(a, b);
  return 0;
}