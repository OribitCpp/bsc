// RUN: %clang %s -o %t.output
// RUN: %t.output
// RUN: %clang -rewrite-bsc %s -o %t-rw.c
// RUN: %clang %t-rw.c -o %t-rw.output
// RUN: %t-rw.output
// expected-no-diagnostics

struct UnaryFunctor<T>{
};
struct DataFrame {};
int struct DataFrame::IsEmpty(const This *this) {
    return 0;
}
int struct UnaryFunctor<T>::ApplyUnaryFunctor(T *uf, struct DataFrame *dst) {
    if (dst->IsEmpty())
        return struct UnaryFunctor<T>::A();
    return 0;
}

int struct UnaryFunctor<T>::A() {
    return 0;
}

int main() {
    int a = 10;
    struct DataFrame d;
    return struct UnaryFunctor<int>::ApplyUnaryFunctor(&a, &d);
}

