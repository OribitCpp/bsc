#include "bsc_generic.hbs"
#include <stdio.h>
#include "user_header.h"

struct UserHBSStruct {
  int a;
  int b;
};

enum UserHBSEnum {
  UFIRST,
  USECOND,
  UDIGIT_END
};

int x = 1;

BSCGenericStruct<T> init<T>(T t) {
  BSCGenericStruct<T> s = { t };
  return s;
}

static void use() {
  BSCGenericStruct<int> s = init<int>(1);
}