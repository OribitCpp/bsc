// RUN: %clang %s -o %test.output
// RUN: %test.output
// RUN: %clang -rewrite-bsc %s -o %t-rw.c
// RUN: %clang %t-rw.c -o %t-rw.output
// RUN: %t-rw.output

int y = 0;
owned struct A {  
 public:
     int x;
     ~A( A this) {
        y++;
    }
  };      
  
int test_if(_Bool c_p2 ) {
    A aaa = {};
    if (!c_p2){
       return 11;
    }
    return 11;
}
int test_if_single(_Bool c_p2) {
     A aaa = {};
    if (!c_p2)
      return 11;
    return 11;
}

int main() {
  test_if(0);
  test_if(1);
  test_if_single(0);
  test_if_single(1);
  return y-4;
}